import { Pencil1Icon } from '@radix-ui/react-icons';
import { Delete, SquareTerminal } from 'lucide-react';

const Icons = {
  Delete: Delete,
  Edit: Pencil1Icon,
  Dashboard: SquareTerminal,
};

export default Icons;
